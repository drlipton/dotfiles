return {
  "tpope/vim-surround",
  keys = { "cs", "ds", "ys" },  -- Load when any of these keys are pressed
  config = function()
    -- No special configuration needed for vim-surround, but you can customize keymaps if you want.
  end,
}
